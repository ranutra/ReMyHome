/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.istockphoto.com', 't3.ftcdn.net', 'pleasant-lapwing-55.convex.cloud', 'upload.wikimedia.org', 'images.unsplash.com']
  },
};

export default nextConfig;
